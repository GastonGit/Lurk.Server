import React from 'react';
import '../styles/VideoPlayer.css';

export default class VideoControls extends React.Component<unknown, unknown> {
    constructor(props: unknown) {
        super(props);
    }

    render(): JSX.Element {
        return <div></div>;
    }
}
